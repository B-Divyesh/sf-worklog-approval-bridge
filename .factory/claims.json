[
  {
    "id": "offline-reload",
    "claim": "Saved work remains available offline after the first visit",
    "where": "app offline state, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; demo data; load once, set context offline, reload"
  },
  {
    "id": "csv-export",
    "claim": "Exports the reviewed worklog as CSV",
    "where": "app export button, README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "demo data; assert CSV header and six data records"
  },
  {
    "id": "local-demo",
    "claim": "The demo sends no worklog data to another origin",
    "where": "landing privacy fact, README",
    "test": "npm test -- --grep @claim:local-demo",
    "sandbox": "fresh browser context; capture all requests through review and approval-link creation"
  },
  {
    "id": "approval-receipt",
    "claim": "A client can accept a shared packet once and download a server-attested receipt",
    "where": "landing how-it-works, README",
    "test": "npm test -- --grep @claim:approval-receipt",
    "sandbox": "demo data; create link, open it, accept, reload the exact URL, and inspect the returned receipt ID and attestation"
  },
  {
    "id": "worklog-details-local",
    "claim": "Worklog details stay on the device; acceptance submits only the packet digest and supplied name",
    "where": "landing facts, privacy section, privacy policy",
    "test": "npm test -- --grep @claim:worklog-details-local",
    "sandbox": "demo data; intercept the same-origin acceptance request and assert its exact two-field body contains no worklog text"
  },
  {
    "id": "no-surveillance",
    "claim": "The product does not request camera, microphone, or screen access",
    "where": "landing facts and privacy section",
    "test": "npm test -- --grep @claim:no-surveillance",
    "sandbox": "fresh demo; deny and record browser permission requests while completing the main flow"
  },
  {
    "id": "calendar-import",
    "claim": "Imports events from an ICS calendar file",
    "where": "landing how-it-works, README",
    "test": "npm test -- --grep @claim:calendar-import",
    "sandbox": "fresh demo; import an in-memory ICS event and assert its summary and duration"
  },
  {
    "id": "git-metadata",
    "claim": "Reads hash, date, and subject metadata from a selected local Git repository",
    "where": "landing privacy section, README",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_git_metadata",
    "sandbox": "temporary Git repository with one local commit; no network"
  },
  {
    "id": "no-repository-upload",
    "claim": "Reading a selected repository does not upload it",
    "where": "landing privacy section, privacy policy",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml claim_no_repository_upload",
    "sandbox": "temporary repository with a configured loopback Git remote; collect metadata and assert no remote connection is opened"
  },
  {
    "id": "license-unlock",
    "claim": "A valid Sociobot license enables Pro features and is cached for one day",
    "where": "pricing, README",
    "test": "npm test -- --grep @claim:license-unlock",
    "sandbox": "fresh app storage; mocked Sociobot verification response; no live checkout or billing"
  },
  {
    "id": "installer-sha256",
    "claim": "The macOS and Linux installer rejects a download whose SHA-256 does not match the published checksum",
    "where": "Download page, README",
    "test": "node --test --test-name-pattern @claim:installer-sha256 scripts/installer-verification.test.mjs",
    "sandbox": "temporary installer fixture; mocked release metadata, asset download, and SHA256SUMS; assert matching bytes install and mismatched bytes are rejected before installation"
  },
  {
    "id": "release-provenance",
    "claim": "Every published desktop platform is tied to the immutable release source commit",
    "where": "Download page, latest.json, README",
    "test": "node --test --test-name-pattern @claim:release-provenance scripts/release-provenance.test.mjs",
    "sandbox": "temporary macOS x64/arm64, Windows, AppImage, and DEB fixtures; record each matrix build checksum and commit; reject one stale platform; assert latest.json covers every downloadable artifact at the nominated commit"
  }
]
